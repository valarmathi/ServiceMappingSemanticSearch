# ServiceMappingSemanticSearch

The main objective of this project is to answer user queries on ServiceNow's features.

The chatbot gets the query from the user and searches the ServiceNow docs semantically to fetch the relevant answer and return the same to the user.

Th code starts with app.py